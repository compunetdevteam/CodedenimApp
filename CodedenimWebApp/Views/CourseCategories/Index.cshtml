@model IEnumerable<CodeninModel.CourseCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutBoard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StudentType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseCategoryId })
        </td>
    </tr>
}

</table>
