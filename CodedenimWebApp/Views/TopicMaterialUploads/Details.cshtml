
@using CodeninModel
@*@using CodedenimWebApp.ViewModels.CourseContentVm*@
@model CodeninModel.TopicMaterialUpload
@*@model CodedenimWebApp.ViewModels.CourseContentVm*@
@{
    ViewBag.Title = "Topic Material Details";

    Layout = "~/Views/Shared/_DashboardLayout.cshtml";

}


    <h2>Details</h2>
<div class="row panel panel-default">

    @Html.Partial("DetailsContent")
</div>
<div>
        @*@foreach (var item in Model.Modules)
        {
            @item.ModuleName
        }*@

    @*<div class="col-xs-12 col-md-8 ">
        @if (Model.FileType == FileType.MP4)
        {
            <div style="text-align:center">
                <button onclick="playPause()">Play/Pause</button>
                <button onclick="makeBig()">Big</button>
                <button onclick="makeSmall()">Small</button>
                <button onclick="makeNormal()">Normal</button>
                <br><br>
            
                <video controls="controls" id="video1" width="420">
                    <source src="~/MaterialUpload/@Model.FileLocation" type="video/mp4">
                    Your browser does not support HTML5 video.
                </video>
            </div>
        }
        else if (Model.FileType == FileType.PDF)
        {
            <object data="~/MaterialUpload/@Model.FileLocation" type="application/pdf">
                <iframe src="~/MaterialUpload/@Model.FileLocation" style="width: 100%;height: 100%;border: none;"> </iframe>


            </object>

        }
        else if (Model.FileType == FileType.MP3)
        {

            <audio controls="controls" id="video1" width="420">
                <source src="~/MaterialUpload/@Model.FileLocation" type="audio/mpeg">

            </audio>

        }
    </div>
    
        <div class="col-xs-6 col-md-4 ">
            <h4>Topic Material Upload</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt class="label label-default">
                    @Html.DisplayNameFor(model => model.Course.TopicName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Course.TopicName)
                </dd>
                <hr />
                <dt class="label label-default">
                    @Html.DisplayNameFor(model => model.Tutor)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Tutor)
                </dd>
                <hr />
                <dt class="label label-default">
                    @Html.DisplayNameFor(model => model.FileType)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.FileType)
                </dd>
                <hr />
                <dt class="label label-default">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>

             
            


            </dl>
        </div>*@

</div>
<script>

    var myVideo = document.getElementById("video1");

    function playPause() {
        if (myVideo.paused)
            myVideo.play();
        else
            myVideo.pause();
    }

    function makeBig() {
        myVideo.width = 560;
    }

    function makeSmall() {
        myVideo.width = 320;
    }

    function makeNormal() {
        myVideo.width = 420;
    }
</script>
